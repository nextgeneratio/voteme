#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    printf("VoteMe: Voting UI placeholder.\n");
    printf("This build does not include an interactive voting UI yet.\n");
    printf("Use the Admin tool (bin/admin) to manage data and run voting logic.\n");
    printf("You can also view results from the main menu (bin/voteme -> option 5).\n");
    return 0;
}
